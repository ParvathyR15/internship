var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import MockAdapter from 'axios-mock-adapter';
import { axios, createFactory } from '..';
const { createInterface: createInterface1 } = createFactory();
const { createInterface: createInterface2 } = createFactory({
    transformDeleteParamsIntoBody: true,
});
const mock = new MockAdapter(axios);
describe('transformDeleteParamsIntoBody', () => {
    test('transformDeleteParamsIntoBody: false', () => __awaiter(void 0, void 0, void 0, function* () {
        const deleteUser = createInterface1('DELETE', '/users/{id}');
        mock.onDelete(/\/users\/\d+/).reply(config => {
            return [
                200,
                {
                    status: 'OK',
                    url: config.url,
                    params: config.params,
                    data: config.data,
                },
            ];
        });
        expect.assertions(1);
        const response = yield deleteUser({ id: 1, name: 'John', gender: 'male' });
        expect(response).toStrictEqual({
            status: 'OK',
            url: '/users/1',
            params: {
                name: 'John',
                gender: 'male',
            },
        });
    }));
    test('transformDeleteParamsIntoBody: true', () => __awaiter(void 0, void 0, void 0, function* () {
        const deleteUser = createInterface2('DELETE', '/users/{id}?name={name}');
        mock.onDelete(/\/users\/\d+/).reply(config => {
            return [
                200,
                {
                    status: 'OK',
                    url: config.url,
                    params: config.params,
                    data: config.data,
                },
            ];
        });
        expect.assertions(1);
        const response = yield deleteUser({ id: 1, name: 'John', gender: 'male' });
        expect(response).toStrictEqual({
            status: 'OK',
            url: '/users/1?name=John',
            data: JSON.stringify({ gender: 'male' }),
        });
    }));
});
